services:
  - type: web
    name: igbot-v5-premium
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --bind 0.0.0.0:$PORT --workers 1 --threads 8
